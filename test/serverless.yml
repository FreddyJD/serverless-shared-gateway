service: therigy-test


plugins:
  - serverless-shared-gateway
  
custom: 
  sharedGateway: 
    gatewayName: therigy-test
    outputResources: true
    outputFilename: '.apigateway.env'
    domainManagerCompatible: true

provider:
  name: aws
  runtime: nodejs8.10
